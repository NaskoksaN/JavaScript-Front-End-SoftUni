function solve(input, word){
    //let result = input.replaceAll(word, '*'.repeat(word.length));
    //let temp = input.split(/\W+/g);
    //let result = temp.map(w => w === word ? '*'.repeat(word.length) : w).join(' ');
    
    console.log(result);
}

solve('A small sentence with some words', 'small');
solve('Find the hidden word', 'hidden');